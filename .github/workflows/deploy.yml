# Deploy the compose stack to the OCI VM.
# Triggered when the build workflow (on `main`) bumps a tag in image-tags.env
# on this `gitops` branch, or on a compose change, or manually.
#
# Flow: copy the stack to the VM over SSH, then run deploy.sh ON the VM, which
# logs into GHCR (creds injected here from a GitHub variable + secret), and
# runs `docker compose pull && up -d`. api.env is maintained by hand on the VM.
name: Deploy

on:
  push:
    branches: [gitops]
    paths:
      - image-tags.env
      - docker-compose.yml
  workflow_dispatch:

concurrency:
  group: dms-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Copy stack to the VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          source: docker-compose.yml,image-tags.env,deploy.sh
          target: /opt/dms

      - name: Deploy on the VM
        uses: appleboy/ssh-action@v1.2.0
        env:
          GHCR_USER: ${{ vars.GHCR_USER }}
          GHCR_TOKEN: ${{ secrets.GHCR_TOKEN }}
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          # Forward the GHCR creds into the remote shell so deploy.sh can log in.
          envs: GHCR_USER,GHCR_TOKEN
          script: |
            set -euo pipefail
            cd /opt/dms
            chmod +x deploy.sh
            ./deploy.sh
